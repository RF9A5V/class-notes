/**
 * Finds the index of the first negative number in an array.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {number} - The index of the first negative number or -1 if none exists.
 */
function findIndexOfFirstNegative(nums) {
  
}

/**
* Finds the index of the first word in an array that is longer than the given length.
* 
* @param {string[]} words - An array of strings.
* @param {number} length - The length to check against.
* @returns {number} - The index of the first word longer than the given length or -1 if none exists.
*/
function findIndexOfWordLongerThan(words, length) {
  
}

module.exports = {
  findIndexOfFirstNegative,
  findIndexOfWordLongerThan
};