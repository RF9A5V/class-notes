/**
 * Increments each number in the array by the given value.
 * 
 * @param {number[]} nums - An array of numbers.
 * @param {number} incrementValue - The value to increment each number by.
 * @returns {number[]} - The modified array with incremented numbers.
 */
function incrementEach(nums, incrementValue) {
  
}

/**
* Converts each string in an array to uppercase.
* 
* @param {string[]} words - An array of strings.
* @returns {string[]} - The modified array with uppercase strings.
*/
function convertToUppercase(words) {

}

module.exports = {
  incrementEach,
  convertToUppercase
};