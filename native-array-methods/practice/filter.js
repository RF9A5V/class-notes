/**
 * Filters out even numbers from an array.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {number[]} - An array containing only the odd numbers.
 */
function filterOutEvens(nums) {
  
}

/**
* Filters out words that start with the letter 'a' from an array of strings.
* 
* @param {string[]} words - An array of strings.
* @returns {string[]} - An array containing words that don't start with the letter 'a'.
*/
function filterOutWordsStartingWithA(words) {
  
}

module.exports = {
  filterOutEvens,
  filterOutWordsStartingWithA
};
