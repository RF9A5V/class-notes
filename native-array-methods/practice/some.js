/**
 * Checks if the array contains any even numbers.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {boolean} - True if the array contains at least one even number, false otherwise.
 */
function hasEvenNumber(nums) {
  
}

/**
* Checks if the array contains any words longer than the given length.
* 
* @param {string[]} words - An array of strings.
* @param {number} length - The length to check against.
* @returns {boolean} - True if the array contains at least one word longer than the given length, false otherwise.
*/
function hasWordLongerThan(words, length) {
  
}

module.exports = {
  hasEvenNumber,
  hasWordLongerThan
};