/**
 * Doubles each number in the array.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {number[]} - An array containing each number doubled.
 */
function doubleEach(nums) {
  
}

/**
* Reverses each string in the array.
* 
* @param {string[]} words - An array of strings.
* @returns {string[]} - An array containing the reversed strings.
*/
function reverseEachWord(words) {
  
}

module.exports = {
  doubleEach,
  reverseEachWord
};