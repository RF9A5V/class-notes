/**
 * Sorts an array of numbers in descending order.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {number[]} - The sorted array in descending order.
 */
function sortDescending(nums) {
  
}

/**
* Sorts an array of strings by the string length (from shortest to longest).
* 
* @param {string[]} words - An array of strings.
* @returns {string[]} - The sorted array by string length.
*/
function sortByLength(words) {
  
}

module.exports = {
  sortDescending,
  sortByLength
};