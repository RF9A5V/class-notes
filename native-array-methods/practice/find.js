/**
 * Finds the first negative number in an array.
 * 
 * @param {number[]} nums - An array of numbers.
 * @returns {number|undefined} - The first negative number or undefined if none exists.
 */
function findFirstNegative(nums) {
  
}

/**
* Finds the first word in an array that is longer than the given length.
* 
* @param {string[]} words - An array of strings.
* @param {number} length - The length to check against.
* @returns {string|undefined} - The first word longer than the given length or undefined if none exists.
*/
function findWordLongerThan(words, length) {
  
}

module.exports = {
  findFirstNegative,
  findWordLongerThan
};